# AI智能医疗问诊平台系统 · 项目操作教学文档

> 本文档带你从零开始，分模块逐步完成 AI 智能医疗问诊平台系统的搭建与部署。
> **核心原则：基础设施即代码** —— Docker、Git 从**第一天**就配置好，中间件（MySQL/Neo4j）用容器跑，每完成一个模块就 Git 提交一次。
> 每个模块都讲清楚：**做什么 → 为什么这么做 → 具体步骤 → 验证方法 → Git 提交**。
> 技术栈：FastAPI + Vue3 + MySQL + Neo4j + Chroma + LangChain + 通义千问

---

## 目录

| 阶段 | 模块 | 核心内容 | Docker 进度 | Git 进度 |
|------|------|---------|------------|---------|
| 一 | 项目准备与基础设施 | 环境、目录、Git初始化、Docker中间件 | MySQL + Neo4j 容器 | 首次提交 |
| 二 | 数据库初始化 | MySQL建表、Neo4j图谱数据 | 用容器里的数据库 | 第二次提交 |
| 三 | 后端基础架构 | 配置/DB会话/统一响应/JWT/入口 | 后端Dockerfile初版 | 第三次提交 |
| 四 | 注册登录模块 | 三角色认证闭环 | 后端容器可登录 | 第四次提交 |
| 五 | 知识图谱模块 | Neo4j建模、症状推理 | 图谱服务容器化 | 第五次提交 |
| 六 | RAG检索增强模块 | 文档加载/分块/向量化/检索 | 向量库持久化 | 第六次提交 |
| 七 | AI智能问诊模块 | SSE流式对话 | 流式接口容器化 | 第七次提交 |
| 八 | 业务功能模块 | 预约/咨询/档案/文章/统计 | 业务接口容器化 | 第八次提交 |
| 九 | 前端工程模块 | Vue3工程、路由、Pinia、页面 | 前端Dockerfile | 第九次提交 |
| 十 | 全量容器化部署 | docker-compose 一键起全套 | 前后端+中间件全容器 | 最终提交 |

---

# 阶段一：项目准备与基础设施

> **本阶段目标：** 把 Git 和 Docker 基础设施搭好，MySQL 和 Neo4j 用 Docker 启动，后面所有开发都基于容器里的中间件。

## 1.1 环境准备

先安装好以下工具：

| 工具 | 版本要求 | 用途 |
|------|---------|------|
| Docker Desktop | 最新版 | 容器化运行 MySQL、Neo4j 等 |
| Git | 最新版 | 版本控制 |
| Python | 3.10+ | 后端开发（本地跑，热更新快） |
| Node.js | 18+ | 前端开发（本地跑，热更新快） |

**验证安装：**
```bash
docker --version    # Docker version 24.x
docker-compose --version  # Docker Compose v2.x
git --version       # git version 2.x
python --version    # Python 3.11.x
node --version      # v18.x 或更高
```

## 1.2 创建项目目录结构

在你的工作目录下，创建如下目录结构。注意：**docker-compose.yml 在项目根目录**，后端代码在 `app/` 目录下模块化组织，前端在 `frontend/` 目录。

```
项目根目录/
├── docker-compose.yml         # Docker编排（第一天就建）
├── .env                       # 环境变量（不进git）
├── .env.example               # 环境变量模板（进git）
├── .gitignore                 # Git忽略规则（第一天就建）
├── .dockerignore              # Docker构建忽略
├── main.py                    # FastAPI 入口文件
├── requirements.txt           # Python 依赖
├── Dockerfile                 # 后端 Docker 镜像（阶段三才建）
├── app/                       # 后端 FastAPI 核心代码
│   ├── core/                  # 核心配置
│   ├── db/                    # 数据库连接
│   ├── models/                # SQLAlchemy 数据模型
│   ├── schemas/               # Pydantic 数据校验
│   ├── services/              # 业务逻辑层
│   ├── rag/                   # RAG 检索增强模块
│   ├── api/v1/                # API 路由（v1 版本）
│   └── utils/                 # 工具函数
├── scripts/                   # 脚本（如初始化图谱）
├── database/
│   └── db_ai_medical.sql
├── knowledge_base/
│   ├── 临床指南/
│   ├── 疾病症状与诊疗/
│   └── 药物信息/
└── frontend/                  # 前端 Vue3（阶段九才建）
    ├── src/
    ├── Dockerfile
    ├── nginx.conf
    └── package.json
```

**Windows PowerShell 创建目录命令：**
```powershell
# 进入项目根目录
cd "你的项目根目录路径"

# 创建后端目录结构
New-Item -ItemType Directory -Force -Path "app\core", "app\db", "app\models", "app\schemas", "app\services", "app\rag", "app\api\v1", "app\utils", "scripts", "database", "knowledge_base\临床指南", "knowledge_base\疾病症状与诊疗", "knowledge_base\药物信息"
```

## 1.3 配置 .gitignore（防密钥泄露，第一天必须建好）

**做什么：** 告诉 Git 哪些文件不需要提交。

**为什么这么做：** 防止 API Key、密码、依赖包、上传文件等敏感或无用文件被提交到远程仓库。**这一步必须在 Git init 之前做好！**

在项目根目录创建 `.gitignore` 文件：

```gitignore
# ===== 敏感信息（最重要！） =====
.env
*.env
!.env.example
*.key
*.pem
*.secret

# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/

# ===== 向量数据库 =====
chroma_db/
*.chroma/

# ===== 上传文件 =====
uploads/
uploads33/
项目uploads文件/

# ===== 前端 =====
node_modules/
dist/
dist-ssr/
*.local

# ===== IDE / 编辑器 =====
.vscode/
.idea/
*.swp
*.swo
*~

# ===== 系统文件 =====
.DS_Store
Thumbs.db
desktop.ini

# ===== 临时文件 =====
*.log
*.tmp
*.bak
~$*.docx
~$*.doc
~$*.xlsx

# ===== 打包文件 =====
*.zip
*.rar
*.7z
*.tar.gz
```

## 1.4 创建 .env.example（配置模板，进 Git）

**做什么：** 提供环境变量的模板文件，别人拿到项目后复制一份改值就能用。

**为什么这么做：** 真实的 `.env` 文件包含密钥不能提交，但项目需要有一个配置说明，所以用 `.env.example` 做模板。

在项目根目录创建 `.env.example`：

```env
# ==========================================
# MySQL 数据库配置
# ==========================================
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=db_ai_medical

# ==========================================
# Neo4j 图数据库配置
# ==========================================
NEO4J_URI=bolt://127.0.0.1:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_neo4j_password

# ==========================================
# 通义千问 API 配置
# ==========================================
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=your_compatible_api_base_url
LLM_MODEL=qwen3.6-plus
EMBEDDING_MODEL=text-embedding-v4

# ==========================================
# JWT 配置（生产环境务必修改为随机字符串）
# ==========================================
JWT_SECRET_KEY=change-this-to-a-long-random-string-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# ==========================================
# 文件上传与向量库
# ==========================================
UPLOAD_DIR=./uploads
CHROMA_PERSIST_DIR=./chroma_db
```

## 1.5 创建 .env（本地真实配置，不进 Git）

把 `.env.example` 复制一份，改名为 `.env`，然后填入你的真实配置：

```bash
# Windows PowerShell
copy .env.example .env

# 然后用编辑器打开 .env，填入真实值
```

`.env` 文件示例（填入真实值）：
```env
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=123456
MYSQL_DATABASE=db_ai_medical

NEO4J_URI=bolt://127.0.0.1:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=12345678

OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx
OPENAI_BASE_URL=https://xxx.cn-beijing.maas.aliyuncs.com/compatible-mode/v1

JWT_SECRET_KEY=ai-medical-consult-secret-key-2026
```

> **重要：** `.env` 文件已经在 `.gitignore` 里了，不会被 Git 提交。但你自己要保管好，不要把它发给别人或传到公开地方。

## 1.6 Docker Compose 基础设施（第一天就启动中间件）

**做什么：** 用 docker-compose 启动 MySQL 和 Neo4j 两个中间件容器。

**为什么这么做：**
1. 不用在本地安装 MySQL 和 Neo4j，减少环境问题
2. 配置统一，团队成员环境一致
3. 后面部署时直接扩展 docker-compose 就行

在项目根目录创建 `docker-compose.yml`：

```yaml
version: '3.8'

services:
  # ==========================================
  # MySQL 数据库
  # ==========================================
  mysql:
    image: mysql:8.0
    container_name: medical-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      TZ: Asia/Shanghai
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database:/docker-entrypoint-initdb.d
    command: >
      --default-authentication-plugin=mysql_native_password
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci

  # ==========================================
  # Neo4j 图数据库
  # ==========================================
  neo4j:
    image: neo4j:5.16-community
    container_name: medical-neo4j
    restart: unless-stopped
    environment:
      NEO4J_AUTH: ${NEO4J_USER}/${NEO4J_PASSWORD}
      NEO4J_dbms_memory_heap_initial__size: 512M
      NEO4J_dbms_memory_heap_max__size: 1G
    ports:
      - "7474:7474"   # Neo4j Browser 网页界面
      - "7687:7687"   # Bolt 协议（程序连接用）
    volumes:
      - neo4j_data:/data

volumes:
  mysql_data:
  neo4j_data:
```

> **代码解释：**
> - `image`：使用的 Docker 镜像
> - `container_name`：容器名字，方便管理
> - `environment`：从 `.env` 文件读取环境变量
> - `ports`：端口映射，左边是宿主机端口，右边是容器内端口
> - `volumes`：数据卷，容器删除后数据还在
> - `./database:/docker-entrypoint-initdb.d`：MySQL 首次启动时会自动执行这个目录里的 `.sql` 文件（建库建表）

## 1.7 启动 Docker 中间件

```bash
# 启动 MySQL 和 Neo4j（后台运行）
docker-compose up -d

# 查看运行状态
docker-compose ps

# 应该看到两个容器都是 Up 状态
# Name              Status    Ports
# ----------------------------------------
# medical-mysql     Up        0.0.0.0:3306->3306/tcp
# medical-neo4j     Up        0.0.0.0:7474->7474/tcp, 0.0.0.0:7687->7687/tcp
```

**验证 MySQL：**
```bash
# 进入 MySQL 容器
docker exec -it medical-mysql mysql -u root -p

# 输入密码后，查看数据库
show databases;
-- 应该能看到 db_ai_medical

exit;
```

**验证 Neo4j：**
打开浏览器访问 `http://localhost:7474`，用 `neo4j / 12345678`（你设置的密码）登录。

## 1.8 Git 初始化与首次提交

现在基础设施搭好了，来做第一次 Git 提交。

```bash
# 初始化 Git 仓库
git init

# 查看状态（确认 .env 不在待提交列表里！）
git status

# 如果看到 .env 被列出来了，说明 .gitignore 没生效，检查一下
```

确认没问题后，执行首次提交：

```bash
# 添加所有文件
git add .

# 提交
git commit -m "阶段一：项目基础设施 - Docker中间件 + Git配置 + 目录结构"

# 查看提交记录
git log
```

## 1.9 连接远程仓库（可选，但建议现在就做）

去 GitHub 或 Gitee 创建一个**私有仓库**，然后关联：

```bash
# 关联远程仓库（地址换成你自己的）
git remote add origin https://github.com/你的用户名/ai-medical-consult-platform.git

# 重命名主分支
git branch -M main

# 推送到远程
git push -u origin main
```

> **为什么建议现在就推？** 代码有远程备份，不怕本地文件丢失。而且私有仓库别人看不到，不用担心。

---

# 阶段二：数据库初始化

> **本阶段目标：** 确认 MySQL 数据表完整，初始化 Neo4j 知识图谱数据。
> **Docker 状态：** MySQL 和 Neo4j 已经在容器里跑着了，直接用。

## 2.1 验证 MySQL 数据表

MySQL 容器首次启动时已经自动执行了 `database/db_ai_medical.sql`，来确认一下：

```bash
# 进入 MySQL 容器
docker exec -it medical-mysql mysql -u root -p db_ai_medical

# 查看所有表
show tables;

-- 应该能看到以下表：
-- t_admin
-- t_appointment
-- t_article
-- t_department
-- t_doctor
-- t_doctor_consult
-- t_doctor_consult_message
-- t_health_record
-- t_knowledge_chunk
-- t_knowledge_file
-- t_notice
-- t_consult_session
-- t_consult_message
-- t_user

# 查看管理员表数据
select * from t_admin;

exit;
```

如果表没创建成功，手动导入：
```bash
# 手动导入 SQL（在项目根目录执行）
docker exec -i medical-mysql mysql -u root -p123456 db_ai_medical < database/db_ai_medical.sql
```

## 2.2 默认测试账号

数据库初始化后，有以下测试账号（密码都是 `123456`）：

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | python222 | 123456 |
| 医生 | doctor1 | 123456 |
| 患者 | user1 | 123456 |

## 2.3 初始化 Neo4j 知识图谱

先创建图谱初始化脚本，后面阶段五会详细讲，这里先把数据导进去。

在 `scripts/` 下创建 `init_graph.py`：

```python
"""初始化知识图谱数据"""
import sys
import os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from neo4j import GraphDatabase

# 直接从环境变量读配置
NEO4J_URI = os.getenv("NEO4J_URI", "bolt://127.0.0.1:7687")
NEO4J_USER = os.getenv("NEO4J_USER", "neo4j")
NEO4J_PASSWORD = os.getenv("NEO4J_PASSWORD", "12345678")


def main():
    driver = GraphDatabase.driver(NEO4J_URI, auth=(NEO4J_USER, NEO4J_PASSWORD))
    
    print("正在初始化知识图谱...")
    
    with driver.session() as session:
        # 创建唯一约束
        session.run("CREATE CONSTRAINT IF NOT EXISTS FOR (d:Disease) REQUIRE d.name IS UNIQUE")
        session.run("CREATE CONSTRAINT IF NOT EXISTS FOR (s:Symptom) REQUIRE s.name IS UNIQUE")
        session.run("CREATE CONSTRAINT IF NOT EXISTS FOR (dr:Drug) REQUIRE dr.name IS UNIQUE")
        session.run("CREATE CONSTRAINT IF NOT EXISTS FOR (dep:Department) REQUIRE dep.name IS UNIQUE")
        print("约束创建完成")

    # 示例医疗数据
    data = [
        {
            "disease": "高血压",
            "symptoms": ["头痛", "头晕", "心悸", "耳鸣", "视力模糊"],
            "drugs": ["硝苯地平", "氨氯地平", "缬沙坦", "厄贝沙坦"],
            "department": "心血管内科"
        },
        {
            "disease": "糖尿病",
            "symptoms": ["多饮", "多尿", "多食", "体重下降", "乏力"],
            "drugs": ["二甲双胍", "格列美脲", "胰岛素"],
            "department": "内分泌科"
        },
        {
            "disease": "冠心病",
            "symptoms": ["胸痛", "胸闷", "心悸", "呼吸困难", "出汗"],
            "drugs": ["阿司匹林", "硝酸甘油", "阿托伐他汀"],
            "department": "心血管内科"
        },
        {
            "disease": "肺炎",
            "symptoms": ["发热", "咳嗽", "咳痰", "胸痛", "呼吸困难"],
            "drugs": ["阿莫西林", "头孢克肟", "左氧氟沙星"],
            "department": "呼吸内科"
        },
        {
            "disease": "慢性胃炎",
            "symptoms": ["胃痛", "胃胀", "反酸", "嗳气", "食欲不振"],
            "drugs": ["奥美拉唑", "雷贝拉唑", "多潘立酮"],
            "department": "消化内科"
        },
        {
            "disease": "甲状腺功能亢进症",
            "symptoms": ["心悸", "多汗", "体重下降", "手抖", "易怒"],
            "drugs": ["甲巯咪唑", "丙硫氧嘧啶"],
            "department": "内分泌科"
        },
        {
            "disease": "慢性肾脏病",
            "symptoms": ["水肿", "乏力", "尿量减少", "恶心", "皮肤瘙痒"],
            "drugs": ["氯沙坦", "氨氯地平", "呋塞米"],
            "department": "肾内科"
        },
        {
            "disease": "慢性阻塞性肺疾病",
            "symptoms": ["咳嗽", "咳痰", "气短", "胸闷", "喘息"],
            "drugs": ["沙丁胺醇", "异丙托溴铵", "布地奈德"],
            "department": "呼吸内科"
        },
        {
            "disease": "急性心肌梗死",
            "symptoms": ["胸痛", "胸闷", "大汗", "呼吸困难", "恶心呕吐"],
            "drugs": ["阿司匹林", "氯吡格雷", "阿托伐他汀", "硝酸甘油"],
            "department": "心血管内科"
        },
    ]

    with driver.session() as session:
        for item in data:
            # 创建疾病节点
            session.run("MERGE (d:Disease {name: $name})", name=item["disease"])
            # 创建症状和关系
            for symptom in item["symptoms"]:
                session.run(
                    "MERGE (s:Symptom {name: $symptom}) "
                    "MERGE (d:Disease {name: $disease}) "
                    "MERGE (d)-[:HAS_SYMPTOM]->(s)",
                    symptom=symptom, disease=item["disease"]
                )
            # 创建药品和关系
            for drug in item["drugs"]:
                session.run(
                    "MERGE (dr:Drug {name: $drug}) "
                    "MERGE (d:Disease {name: $disease}) "
                    "MERGE (d)-[:RECOMMEND_DRUG]->(dr)",
                    drug=drug, disease=item["disease"]
                )
            # 创建科室和关系
            session.run(
                "MERGE (dep:Department {name: $dept}) "
                "MERGE (d:Disease {name: $disease}) "
                "MERGE (d)-[:BELONGS_TO]->(dep)",
                dept=item["department"], disease=item["disease"]
            )
            print(f"已添加: {item['disease']}")

    print("知识图谱初始化完成！")
    driver.close()


if __name__ == "__main__":
    main()
```

运行脚本前先装 neo4j 驱动：
```bash
cd .
pip install neo4j python-dotenv
```

运行初始化脚本：
```bash
# Windows PowerShell 加载 .env 变量
$env:NEO4J_URI="bolt://127.0.0.1:7687"
$env:NEO4J_USER="neo4j"
$env:NEO4J_PASSWORD="12345678"

python scripts/init_graph.py
```

**验证图谱：**
打开 Neo4j Browser（http://localhost:7474），执行：
```cypher
MATCH (n) RETURN count(n) as node_count;
-- 应该有几十个节点

MATCH (n)-[r]->(m) RETURN count(r) as relation_count;
-- 应该有上百条关系
```

## 2.4 Git 提交

```bash
git add .
git commit -m "阶段二：数据库初始化 - MySQL表结构 + Neo4j图谱数据"
git push
```

---

# 阶段三：后端基础架构

> **本阶段目标：** 搭建 FastAPI 后端的基础框架，包括配置、数据库连接、统一响应、JWT、依赖注入、应用入口。
> **Docker 状态：** 本阶段末创建后端 Dockerfile 初版，验证后端能在容器里跑起来。

## 3.1 后端依赖

在项目根目录下创建 `requirements.txt`：

```
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy>=2.0.0
pymysql>=1.1.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.10
chromadb>=0.4.0
neo4j>=5.15.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-docx>=1.1.0
PyPDF2>=3.0.0
markdown>=3.5
aiofiles>=23.0.0
httpx>=0.25.0
python-dotenv>=1.0.0
```

安装依赖：
```bash
cd .
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 3.2 配置模块（core/config.py）

**做什么：** 集中管理所有配置，从环境变量读取敏感信息。

在 `app/core/` 下创建 `config.py`：

```python
"""核心配置模块 - 集中管理项目配置"""
import os
from pathlib import Path
from dotenv import load_dotenv

# 加载 .env 文件（从项目根目录找）
BASE_DIR = Path(__file__).resolve().parent.parent  # app/ 目录
PROJECT_ROOT = BASE_DIR.parent  # 项目根目录（docker-compose.yml 所在目录）
load_dotenv(PROJECT_ROOT / ".env")

# 项目名称
PROJECT_NAME = "AI智能医疗问诊平台系统"

# MySQL数据库配置（从环境变量读取）
MYSQL_HOST = os.getenv("MYSQL_HOST", "127.0.0.1")
MYSQL_PORT = int(os.getenv("MYSQL_PORT", "3306"))
MYSQL_USER = os.getenv("MYSQL_USER", "root")
MYSQL_PASSWORD = os.getenv("MYSQL_PASSWORD", "123456")
MYSQL_DATABASE = os.getenv("MYSQL_DATABASE", "db_ai_medical")
DATABASE_URL = f"mysql+pymysql://{MYSQL_USER}:{MYSQL_PASSWORD}@{MYSQL_HOST}:{MYSQL_PORT}/{MYSQL_DATABASE}?charset=utf8mb4"

# Neo4j知识图谱配置
NEO4J_URI = os.getenv("NEO4J_URI", "bolt://127.0.0.1:7687")
NEO4J_USER = os.getenv("NEO4J_USER", "neo4j")
NEO4J_PASSWORD = os.getenv("NEO4J_PASSWORD", "12345678")

# LLM大模型配置
OPENAI_API_KEY = os.getenv("OPENAI_API_KEY", "")
OPENAI_BASE_URL = os.getenv("OPENAI_BASE_URL", "")
LLM_MODEL = os.getenv("LLM_MODEL", "qwen3.6-plus")
EMBEDDING_MODEL = os.getenv("EMBEDDING_MODEL", "text-embedding-v4")
EMBEDDING_DIMENSIONS = int(os.getenv("EMBEDDING_DIMENSIONS", "2048"))
EMBEDDING_BATCH_SIZE = 10

# 文件上传目录
UPLOAD_DIR = os.getenv("UPLOAD_DIR", str(BASE_DIR / "uploads"))

# Chroma向量数据库目录
CHROMA_PERSIST_DIR = os.getenv("CHROMA_PERSIST_DIR", str(BASE_DIR / "chroma_db"))
CHROMA_COLLECTION = "medical_knowledge"

# JWT配置
JWT_SECRET_KEY = os.getenv("JWT_SECRET_KEY", "ai-medical-consult-secret-key-2026")
JWT_ALGORITHM = os.getenv("JWT_ALGORITHM", "HS256")
JWT_EXPIRE_MINUTES = int(os.getenv("JWT_EXPIRE_MINUTES", "1440"))

# RAG配置
CHUNK_SIZE = 500
CHUNK_OVERLAP = 80
RETRIEVAL_TOP_K = 5

# 启动时检查API Key
if not OPENAI_API_KEY:
    print("[警告] 未检测到环境变量 OPENAI_API_KEY，LLM和向量化功能将无法正常使用！")


class Settings:
    """配置类 - 统一访问入口"""
    project_name: str = PROJECT_NAME
    base_dir: Path = BASE_DIR
    database_url: str = DATABASE_URL
    neo4j_uri: str = NEO4J_URI
    neo4j_user: str = NEO4J_USER
    neo4j_password: str = NEO4J_PASSWORD
    openai_api_key: str = OPENAI_API_KEY
    openai_base_url: str = OPENAI_BASE_URL
    llm_model: str = LLM_MODEL
    embedding_model: str = EMBEDDING_MODEL
    embedding_dimensions: int = EMBEDDING_DIMENSIONS
    embedding_batch_size: int = EMBEDDING_BATCH_SIZE
    upload_dir: str = UPLOAD_DIR
    chroma_persist_dir: str = CHROMA_PERSIST_DIR
    chroma_collection: str = CHROMA_COLLECTION
    jwt_secret_key: str = JWT_SECRET_KEY
    jwt_algorithm: str = JWT_ALGORITHM
    jwt_expire_minutes: int = JWT_EXPIRE_MINUTES
    chunk_size: int = CHUNK_SIZE
    chunk_overlap: int = CHUNK_OVERLAP
    retrieval_top_k: int = RETRIEVAL_TOP_K


settings = Settings()
```

在 `core/__init__.py` 中：
```python
# core 包初始化
```

> **为什么用 python-dotenv？** 它会自动读取项目根目录的 `.env` 文件，把里面的配置加载到环境变量里，这样代码里用 `os.getenv()` 就能读到。开发时本地跑和 Docker 容器里跑都能用同一套代码。

## 3.3 数据库会话（db/session.py）

在 `app/db/` 下创建 `session.py`：

```python
"""数据库会话模块"""
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, declarative_base

from app.core.config import settings

engine = create_engine(
    settings.database_url,
    pool_pre_ping=True,
    pool_recycle=3600,
    echo=False,
)

SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
Base = declarative_base()


def get_db():
    """获取数据库会话（依赖注入用）"""
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

创建 `db/base.py`：
```python
"""数据库基类"""
from app.db.session import Base

__all__ = ["Base"]
```

创建 `db/__init__.py`：
```python
# db 包初始化
```

## 3.4 统一响应格式（core/response.py）

在 `core/response.py` 中：

```python
"""统一响应格式"""
from typing import Any


def success(data: Any = None, message: str = "操作成功") -> dict:
    """成功响应"""
    return {"code": 200, "message": message, "data": data}


def error(message: str = "操作失败", code: int = 400) -> dict:
    """失败响应"""
    return {"code": code, "message": message, "data": None}


def page_result(items: list, total: int, page: int = 1, page_size: int = 10) -> dict:
    """分页响应"""
    return success({
        "items": items,
        "total": total,
        "page": page,
        "page_size": page_size,
    })
```

## 3.5 JWT 安全模块（core/security.py）

在 `core/security.py` 中：

```python
"""安全模块 - JWT令牌与密码验证"""
from datetime import datetime, timedelta
from typing import Optional

from jose import JWTError, jwt

from app.core.config import settings


def verify_password(plain_password: str, stored_password: str) -> bool:
    """验证密码（明文比对）"""
    return plain_password == stored_password


def create_access_token(data: dict, expires_delta: Optional[timedelta] = None) -> str:
    """创建JWT访问令牌"""
    to_encode = data.copy()
    expire = datetime.utcnow() + (expires_delta or timedelta(minutes=settings.jwt_expire_minutes))
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, settings.jwt_secret_key, algorithm=settings.jwt_algorithm)


def decode_access_token(token: str) -> Optional[dict]:
    """解码JWT令牌"""
    try:
        payload = jwt.decode(token, settings.jwt_secret_key, algorithms=[settings.jwt_algorithm])
        return payload
    except JWTError:
        return None
```

## 3.6 依赖注入模块（core/deps.py）

在 `core/deps.py` 中：

```python
"""依赖注入模块 - 当前用户与角色守卫"""
from typing import Optional

from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPAuthorizationCredentials, HTTPBearer
from sqlalchemy.orm import Session

from app.core.security import decode_access_token
from app.db.session import get_db

security = HTTPBearer(auto_error=False)


class CurrentUser:
    """当前登录用户信息"""
    def __init__(self, user_id: int, username: str, role: str, obj=None):
        self.user_id = user_id
        self.username = username
        self.role = role
        self.obj = obj


def get_current_user(
    credentials: Optional[HTTPAuthorizationCredentials] = Depends(security),
    db: Session = Depends(get_db),
) -> CurrentUser:
    """获取当前登录用户（需要登录才能访问的接口用这个）"""
    if not credentials:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="未登录")
    payload = decode_access_token(credentials.credentials)
    if not payload:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="令牌无效或已过期")
    user_id = payload.get("user_id")
    username = payload.get("sub")
    role = payload.get("role")
    if not user_id or not role:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="令牌数据不完整")
    return CurrentUser(user_id=user_id, username=username, role=role)


def require_roles(*roles: str):
    """角色守卫 - 校验当前用户角色是否有权限"""
    def role_checker(current: CurrentUser = Depends(get_current_user)) -> CurrentUser:
        if current.role not in roles:
            raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="权限不足")
        return current
    return role_checker
```

## 3.7 工具函数（utils/）

创建 `utils/__init__.py` 和 `utils/validation.py`：

```python
"""参数校验错误格式化工具"""
def format_validation_errors(errors: list) -> str:
    """将FastAPI参数校验错误转为中文提示"""
    messages = []
    for err in errors:
        field = ".".join(str(loc) for loc in err.get("loc", []) if loc != "body")
        msg = err.get("msg", "参数错误")
        if field:
            messages.append(f"{field}: {msg}")
        else:
            messages.append(msg)
    return "；".join(messages)
```

创建 `utils/helpers.py`：

```python
"""通用工具函数"""
from datetime import datetime


def format_datetime(dt) -> str:
    """格式化日期时间为 YYYY-MM-DD HH:mm:ss"""
    if not dt:
        return ""
    if isinstance(dt, datetime):
        return dt.strftime("%Y-%m-%d %H:%M:%S")
    return str(dt)


def format_date(dt) -> str:
    """格式化日期为 YYYY-MM-DD"""
    if not dt:
        return ""
    if isinstance(dt, datetime):
        return dt.strftime("%Y-%m-%d")
    return str(dt)
```

## 3.8 应用入口（main.py）

在项目根目录下创建 `main.py`：

```python
"""AI智能医疗问诊平台系统 - FastAPI应用入口"""
import os
from contextlib import asynccontextmanager

from fastapi import FastAPI, HTTPException, Request
from fastapi.exceptions import RequestValidationError
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import JSONResponse
from fastapi.staticfiles import StaticFiles

from app.core.config import settings
from app.utils.validation import format_validation_errors


@asynccontextmanager
async def lifespan(app: FastAPI):
    """应用生命周期：启动时初始化目录"""
    os.makedirs(settings.upload_dir, exist_ok=True)
    os.makedirs(os.path.join(settings.upload_dir, "knowledge"), exist_ok=True)
    os.makedirs(os.path.join(settings.upload_dir, "avatar"), exist_ok=True)
    os.makedirs(settings.chroma_persist_dir, exist_ok=True)
    print(f"[启动] {settings.project_name} 服务已就绪")
    if not settings.openai_api_key:
        print("[警告] OPENAI_API_KEY 未设置，LLM功能不可用")
    yield


app = FastAPI(
    title=settings.project_name,
    description="基于RAG+LangChain+Neo4j的AI智能医疗问诊平台",
    version="1.0.0",
    lifespan=lifespan,
)


@app.exception_handler(HTTPException)
async def http_exception_handler(request: Request, exc: HTTPException):
    """统一 HTTP 异常响应格式"""
    detail = exc.detail
    if isinstance(detail, str):
        message = detail
    elif isinstance(detail, list):
        message = "；".join(str(item) for item in detail)
    else:
        message = str(detail)
    return JSONResponse(
        status_code=exc.status_code,
        content={"code": exc.status_code, "message": message, "data": None},
    )


@app.exception_handler(RequestValidationError)
async def validation_exception_handler(request: Request, exc: RequestValidationError):
    """统一参数校验错误为中文提示"""
    message = format_validation_errors(exc.errors())
    return JSONResponse(
        status_code=422,
        content={"code": 422, "message": message, "data": None},
    )

# CORS跨域
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 静态文件挂载
if os.path.exists(settings.upload_dir):
    app.mount("/uploads33", StaticFiles(directory=settings.upload_dir), name="uploads")


@app.get("/")
async def root():
    """根路径"""
    return {"message": f"欢迎使用{settings.project_name}", "docs": "/docs"}


@app.get("/health")
async def health_check():
    """健康检查接口（Docker用）"""
    return {"status": "ok"}
```

## 3.9 本地验证后端

```bash
cd .
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

打开浏览器访问：
- `http://localhost:8000/` → 看到欢迎信息
- `http://localhost:8000/docs` → 看到 Swagger 文档
- `http://localhost:8000/health` → 返回 `{"status":"ok"}`

## 3.10 后端 Dockerfile（本阶段加入 Docker）

**做什么：** 创建后端的 Docker 镜像构建文件。

**为什么现在就做？** 尽早验证后端能在容器里跑，避免到最后部署时才发现一堆环境问题。

在项目根目录下创建 `Dockerfile`：

```dockerfile
# ==========================================
# 后端 Dockerfile
# ==========================================
FROM python:3.11-slim

WORKDIR /app

# 安装系统依赖（编译 chromadb 等需要）
RUN apt-get update && apt-get install -y \
    gcc \
    g++ \
    && rm -rf /var/lib/apt/lists/*

# 先复制依赖文件，利用 Docker 缓存层
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 复制源码
COPY . .

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## 3.11 更新 docker-compose.yml，加入后端服务

在项目根目录的 `docker-compose.yml` 中，添加后端服务（加在 neo4j 服务后面，volumes 前面）：

```yaml
  # ==========================================
  # 后端服务
  # ==========================================
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: medical-server
    restart: unless-stopped
    depends_on:
      - mysql
      - neo4j
    environment:
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=${NEO4J_USER}
      - NEO4J_PASSWORD=${NEO4J_PASSWORD}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL}
      - LLM_MODEL=${LLM_MODEL}
      - EMBEDDING_MODEL=${EMBEDDING_MODEL}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - JWT_ALGORITHM=${JWT_ALGORITHM}
      - JWT_EXPIRE_MINUTES=${JWT_EXPIRE_MINUTES}
      - UPLOAD_DIR=/app/uploads
      - CHROMA_PERSIST_DIR=/app/chroma_db
    ports:
      - "8000:8000"
    volumes:
      - uploads_data:/app/uploads
      - chroma_data:/app/chroma_db
```

同时在底部的 `volumes:` 里加上两个新卷：

```yaml
volumes:
  mysql_data:
  neo4j_data:
  uploads_data:    # 新增
  chroma_data:     # 新增
```

## 3.12 构建并启动后端容器

```bash
# 构建后端镜像并启动
docker-compose up -d --build server

# 查看日志，确认启动成功
docker-compose logs -f server

# 看到类似输出就说明成功了：
# [启动] AI智能医疗问诊平台系统 服务已就绪
# INFO:     Uvicorn running on http://0.0.0.0:8000
```

按 `Ctrl+C` 退出日志查看。访问 `http://localhost:8000/docs` 验证。

## 3.13 Git 提交

```bash
git add .
git commit -m "阶段三：后端基础架构 - 配置/DB/JWT/统一响应/入口 + 后端Docker化"
git push
```

---

# 阶段四：注册登录模块

> **本阶段目标：** 实现三角色（患者/医生/管理员）的注册和登录功能。
> **Docker 状态：** 后端已经容器化，写完代码重新构建镜像验证。

## 4.1 数据模型（models/）

创建三个模型文件。

**models/user.py（患者用户表）：**
```python
"""用户模型 - 患者"""
from sqlalchemy import Column, Integer, String, DateTime, Text, SmallInteger
from sqlalchemy.sql import func

from app.db.session import Base


class User(Base):
    __tablename__ = "t_user"

    id = Column(Integer, primary_key=True, autoincrement=True, comment="主键ID")
    username = Column(String(50), unique=True, nullable=False, comment="用户名")
    password = Column(String(100), nullable=False, comment="密码")
    real_name = Column(String(50), comment="真实姓名")
    gender = Column(SmallInteger, default=1, comment="性别:1男2女")
    phone = Column(String(20), comment="手机号")
    email = Column(String(100), comment="邮箱")
    avatar = Column(String(255), comment="头像路径")
    birthday = Column(String(20), comment="生日")
    address = Column(String(255), comment="地址")
    status = Column(SmallInteger, default=1, comment="状态:1正常0禁用")
    create_time = Column(DateTime, server_default=func.now(), comment="创建时间")
    update_time = Column(DateTime, server_default=func.now(), onupdate=func.now(), comment="更新时间")
```

**models/doctor.py（医生表）：**
```python
"""医生模型"""
from sqlalchemy import Column, Integer, String, DateTime, Text, SmallInteger, Float
from sqlalchemy.sql import func

from app.db.session import Base


class Doctor(Base):
    __tablename__ = "t_doctor"

    id = Column(Integer, primary_key=True, autoincrement=True, comment="主键ID")
    username = Column(String(50), unique=True, nullable=False, comment="用户名")
    password = Column(String(100), nullable=False, comment="密码")
    real_name = Column(String(50), comment="真实姓名")
    gender = Column(SmallInteger, default=1, comment="性别:1男2女")
    phone = Column(String(20), comment="手机号")
    email = Column(String(100), comment="邮箱")
    avatar = Column(String(255), comment="头像路径")
    department_id = Column(Integer, comment="科室ID")
    title = Column(String(50), comment="职称")
    specialty = Column(String(500), comment="擅长")
    introduction = Column(Text, comment="简介")
    fee = Column(Float, default=0, comment="咨询费")
    status = Column(SmallInteger, default=1, comment="状态:1正常0禁用")
    create_time = Column(DateTime, server_default=func.now(), comment="创建时间")
    update_time = Column(DateTime, server_default=func.now(), onupdate=func.now(), comment="更新时间")
```

**models/admin.py（管理员表）：**
```python
"""管理员模型"""
from sqlalchemy import Column, Integer, String, DateTime, SmallInteger
from sqlalchemy.sql import func

from app.db.session import Base


class Admin(Base):
    __tablename__ = "t_admin"

    id = Column(Integer, primary_key=True, autoincrement=True, comment="主键ID")
    username = Column(String(50), unique=True, nullable=False, comment="用户名")
    password = Column(String(100), nullable=False, comment="密码")
    nickname = Column(String(50), comment="昵称")
    avatar = Column(String(255), comment="头像路径")
    phone = Column(String(20), comment="手机号")
    email = Column(String(100), comment="邮箱")
    status = Column(SmallInteger, default=1, comment="状态:1正常0禁用")
    create_time = Column(DateTime, server_default=func.now(), comment="创建时间")
    update_time = Column(DateTime, server_default=func.now(), onupdate=func.now(), comment="更新时间")
```

**models/__init__.py：**
```python
from app.models.user import User
from app.models.doctor import Doctor
from app.models.admin import Admin

__all__ = ["User", "Doctor", "Admin"]
```

## 4.2 Pydantic 校验模型（schemas/common.py）

创建 `schemas/__init__.py` 和 `schemas/common.py`：

```python
"""通用Pydantic模型 - 请求和响应数据校验"""
from typing import Optional
from pydantic import BaseModel, Field


class LoginRequest(BaseModel):
    """登录请求"""
    username: str = Field(..., description="用户名")
    password: str = Field(..., description="密码")
    role: str = Field(..., description="角色: user/doctor/admin")


class RegisterRequest(BaseModel):
    """注册请求 - 患者注册"""
    username: str = Field(..., min_length=3, max_length=50, description="用户名")
    password: str = Field(..., min_length=6, max_length=50, description="密码")
    confirm_password: str = Field(..., description="确认密码")
    real_name: Optional[str] = Field(None, max_length=50, description="真实姓名")
    phone: Optional[str] = Field(None, max_length=20, description="手机号")


class TokenResponse(BaseModel):
    """登录/注册成功返回"""
    access_token: str
    role: str
    user_id: int
    username: str
    nickname: Optional[str] = None
    avatar: Optional[str] = None
```

## 4.3 认证服务（services/auth_service.py）

创建 `services/__init__.py` 和 `services/auth_service.py`：

```python
"""认证服务 - 登录注册业务逻辑"""
from fastapi import HTTPException
from sqlalchemy.orm import Session

from app.core.security import verify_password, create_access_token
from app.models.admin import Admin
from app.models.doctor import Doctor
from app.models.user import User
from app.schemas.common import LoginRequest, RegisterRequest, TokenResponse


class AuthService:
    """认证业务逻辑"""

    @staticmethod
    def login(db: Session, req: LoginRequest) -> TokenResponse:
        """三角色登录"""
        # 根据角色查不同的表
        if req.role == "admin":
            user = db.query(Admin).filter(Admin.username == req.username).first()
            nickname = user.nickname if user else None
        elif req.role == "doctor":
            user = db.query(Doctor).filter(Doctor.username == req.username).first()
            nickname = user.real_name if user else None
        elif req.role == "user":
            user = db.query(User).filter(User.username == req.username).first()
            nickname = user.real_name if user else None
        else:
            raise HTTPException(status_code=400, detail="无效的角色类型")

        if not user or not verify_password(req.password, user.password):
            raise HTTPException(status_code=401, detail="用户名或密码错误")
        if hasattr(user, "status") and user.status == 0:
            raise HTTPException(status_code=403, detail="账号已被禁用")

        # 生成JWT令牌
        token = create_access_token({
            "sub": user.username,
            "user_id": user.id,
            "role": req.role,
        })
        return TokenResponse(
            access_token=token,
            role=req.role,
            user_id=user.id,
            username=user.username,
            nickname=nickname,
            avatar=getattr(user, "avatar", None),
        )

    @staticmethod
    def register(db: Session, req: RegisterRequest) -> TokenResponse:
        """患者注册（含确认密码校验）"""
        if req.password != req.confirm_password:
            raise HTTPException(status_code=400, detail="两次密码输入不一致")
        existing = db.query(User).filter(User.username == req.username).first()
        if existing:
            raise HTTPException(status_code=400, detail="用户名已存在")

        user = User(
            username=req.username,
            password=req.password,
            real_name=req.real_name,
            phone=req.phone,
            gender=1,
        )
        db.add(user)
        db.commit()
        db.refresh(user)

        # 注册成功直接登录
        token = create_access_token({"sub": user.username, "user_id": user.id, "role": "user"})
        return TokenResponse(
            access_token=token,
            role="user",
            user_id=user.id,
            username=user.username,
            nickname=user.real_name,
        )
```

## 4.4 认证接口（api/v1/auth.py）

创建 `api/__init__.py`、`api/v1/__init__.py` 和 `api/v1/auth.py`：

```python
"""认证接口"""
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session

from app.core.response import success
from app.db.session import get_db
from app.schemas.common import LoginRequest, RegisterRequest
from app.services.auth_service import AuthService

router = APIRouter()


@router.post("/login")
def login(req: LoginRequest, db: Session = Depends(get_db)):
    """三角色登录"""
    result = AuthService.login(db, req)
    return success(result.model_dump())


@router.post("/register")
def register(req: RegisterRequest, db: Session = Depends(get_db)):
    """患者注册（含确认密码校验）"""
    result = AuthService.register(db, req)
    return success(result.model_dump())
```

## 4.5 路由汇总（api/v1/__init__.py）

```python
"""API v1 路由汇总"""
from fastapi import APIRouter
from app.api.v1.auth import router as auth_router

api_router = APIRouter(prefix="/api/v1")

api_router.include_router(auth_router, prefix="/auth", tags=["认证"])
```

## 4.6 在 main.py 中注册路由

打开 `main.py`，在 `# 注册API路由` 的位置加上：

```python
from app.api.v1 import api_router

# 注册API路由（加在静态文件挂载后面）
app.include_router(api_router)
```

## 4.7 本地验证

```bash
cd .
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

打开 Swagger 文档 `http://localhost:8000/docs`：

1. 测试登录：`POST /api/v1/auth/login`
   ```json
   {
     "username": "python222",
     "password": "123456",
     "role": "admin"
   }
   ```
   应该返回 token。

2. 测试注册：`POST /api/v1/auth/register`
   ```json
   {
     "username": "testuser001",
     "password": "123456",
     "confirm_password": "123456",
     "real_name": "测试用户",
     "phone": "13800000001"
   }
   ```

## 4.8 Docker 验证

代码没问题了，重新构建后端镜像验证：

```bash
# 重新构建并启动后端
docker-compose up -d --build server

# 查看日志
docker-compose logs -f server

# 确认启动成功后，用 Swagger 测试登录接口
```

## 4.9 Git 提交

```bash
git add .
git commit -m "阶段四：注册登录模块 - 三角色认证 + JWT + 确认密码校验"
git push
```

---

# 阶段五：知识图谱模块

> **本阶段目标：** 封装 Neo4j 图谱服务，实现症状自查和图谱可视化接口。
> **Docker 状态：** Neo4j 已经在容器里，后端服务也容器化了，直接在后端里加图谱服务即可。

## 5.1 科室模型（models/department.py）

```python
"""科室模型"""
from sqlalchemy import Column, Integer, String, DateTime, SmallInteger
from sqlalchemy.sql import func

from app.db.session import Base


class Department(Base):
    __tablename__ = "t_department"

    id = Column(Integer, primary_key=True, autoincrement=True)
    name = Column(String(50), nullable=False, comment="科室名称")
    description = Column(String(500), comment="科室描述")
    sort = Column(Integer, default=0, comment="排序")
    status = Column(SmallInteger, default=1, comment="状态")
    create_time = Column(DateTime, server_default=func.now())
    update_time = Column(DateTime, server_default=func.now(), onupdate=func.now())
```

更新 `models/__init__.py`，加上 `Department`。

## 5.2 图谱服务（services/graph_service.py）

```python
"""知识图谱服务 - Neo4j操作"""
from neo4j import GraphDatabase
from app.core.config import settings


class GraphService:
    """知识图谱业务逻辑"""

    def __init__(self):
        self.driver = GraphDatabase.driver(
            settings.neo4j_uri,
            auth=(settings.neo4j_user, settings.neo4j_password),
        )

    def close(self):
        self.driver.close()

    def infer_diseases_by_symptoms(self, symptom_list):
        """根据症状列表推理可能的疾病（症状共现统计）"""
        with self.driver.session() as session:
            result = session.run(
                "MATCH (d:Disease)-[:HAS_SYMPTOM]->(s:Symptom) "
                "WHERE s.name IN $symptoms "
                "WITH d, count(s) AS match_count, collect(s.name) AS matched_symptoms "
                "ORDER BY match_count DESC "
                "LIMIT 10 "
                "OPTIONAL MATCH (d)-[:BELONGS_TO]->(dep:Department) "
                "RETURN d.name AS disease, match_count, matched_symptoms, dep.name AS department",
                symptoms=symptom_list
            )
            return [
                {
                    "disease": record["disease"],
                    "match_count": record["match_count"],
                    "matched_symptoms": record["matched_symptoms"],
                    "department": record["department"],
                }
                for record in result
            ]

    def get_disease_detail(self, disease_name):
        """获取疾病详情：症状、推荐药品、所属科室"""
        with self.driver.session() as session:
            result = session.run(
                "MATCH (d:Disease {name: $name}) "
                "OPTIONAL MATCH (d)-[:HAS_SYMPTOM]->(s:Symptom) "
                "OPTIONAL MATCH (d)-[:RECOMMEND_DRUG]->(dr:Drug) "
                "OPTIONAL MATCH (d)-[:BELONGS_TO]->(dep:Department) "
                "RETURN d.name AS disease, "
                "collect(DISTINCT s.name) AS symptoms, "
                "collect(DISTINCT dr.name) AS drugs, "
                "dep.name AS department",
                name=disease_name
            )
            record = result.single()
            if not record:
                return None
            return {
                "disease": record["disease"],
                "symptoms": record["symptoms"],
                "drugs": record["drugs"],
                "department": record["department"],
            }

    def get_graph_data(self, limit=100):
        """获取图谱数据用于前端可视化"""
        with self.driver.session() as session:
            result = session.run(
                "MATCH (n)-[r]->(m) "
                "RETURN n, r, m "
                "LIMIT $limit",
                limit=limit
            )
            nodes = {}
            edges = []
            for record in result:
                n = record["n"]
                m = record["m"]
                r = record["r"]
                for node in [n, m]:
                    nid = str(node.element_id)
                    if nid not in nodes:
                        nodes[nid] = {
                            "id": nid,
                            "name": node["name"],
                            "category": list(node.labels)[0],
                        }
                edges.append({
                    "source": str(n.element_id),
                    "target": str(m.element_id),
                    "relation": type(r).__name__,
                })
            return {"nodes": list(nodes.values()), "edges": edges}

    def get_all_symptoms(self):
        """获取所有症状列表（用于症状选择）"""
        with self.driver.session() as session:
            result = session.run("MATCH (s:Symptom) RETURN s.name AS name ORDER BY s.name")
            return [record["name"] for record in result]


_graph_service = None


def get_graph_service():
    """单例获取图谱服务"""
    global _graph_service
    if _graph_service is None:
        _graph_service = GraphService()
    return _graph_service
```

## 5.3 图谱接口（api/v1/graph.py）

```python
"""知识图谱接口"""
from fastapi import APIRouter
from pydantic import BaseModel

from app.core.response import success
from app.services.graph_service import get_graph_service

router = APIRouter()


class SymptomCheckRequest(BaseModel):
    symptoms: list[str]


@router.get("/symptoms")
def get_symptoms():
    """获取所有症状列表"""
    data = get_graph_service().get_all_symptoms()
    return success(data)


@router.post("/symptom-check")
def symptom_check(req: SymptomCheckRequest):
    """症状自查 - 根据输入的症状推理可能疾病"""
    result = get_graph_service().infer_diseases_by_symptoms(req.symptoms)
    return success(result)


@router.get("/disease/{name}")
def disease_detail(name: str):
    """获取疾病详情"""
    data = get_graph_service().get_disease_detail(name)
    return success(data)


@router.get("/visualize")
def visualize(limit: int = 100):
    """知识图谱可视化数据"""
    data = get_graph_service().get_graph_data(limit)
    return success(data)
```

## 5.4 注册路由

在 `api/v1/__init__.py` 中加上：

```python
from app.api.v1.graph import router as graph_router

api_router.include_router(graph_router, prefix="/graph", tags=["知识图谱"])
```

## 5.5 本地验证

启动后端，用 Swagger 测试：

1. `GET /api/v1/graph/symptoms` → 返回所有症状列表
2. `POST /api/v1/graph/symptom-check`
   ```json
   {
     "symptoms": ["头痛", "头晕", "心悸"]
   }
   ```
   → 返回匹配的疾病列表，按匹配数排序

3. `GET /api/v1/graph/visualize` → 返回节点和边的数据

## 5.6 Docker 验证

```bash
docker-compose up -d --build server
docker-compose logs -f server
```

确认启动成功后，用上面同样的接口测试。

## 5.7 Git 提交

```bash
git add .
git commit -m "阶段五：知识图谱模块 - Neo4j症状推理 + 图谱可视化接口"
git push
```

---

# 阶段六：RAG 检索增强模块

> **本阶段目标：** 实现文档加载、文本分块、向量化、向量检索的完整 RAG 链路。
> **Docker 状态：** Chroma 向量数据用 Docker volume 持久化。

## 6.1 知识库模型（models/knowledge.py）

```python
"""知识库模型"""
from sqlalchemy import Column, Integer, String, DateTime, Text, SmallInteger
from sqlalchemy.sql import func

from app.db.session import Base


class KnowledgeFile(Base):
    """知识库文件表"""
    __tablename__ = "t_knowledge_file"

    id = Column(Integer, primary_key=True, autoincrement=True)
    file_name = Column(String(255), nullable=False, comment="文件名")
    file_path = Column(String(500), comment="文件路径")
    file_size = Column(Integer, default=0, comment="文件大小(字节)")
    file_type = Column(String(20), comment="文件类型")
    chunk_count = Column(Integer, default=0, comment="分块数量")
    vector_status = Column(SmallInteger, default=0, comment="向量化状态:0未开始1处理中2成功3失败")
    error_msg = Column(String(500), comment="错误信息")
    create_time = Column(DateTime, server_default=func.now())
    update_time = Column(DateTime, server_default=func.now(), onupdate=func.now())


class KnowledgeChunk(Base):
    """知识库分块表"""
    __tablename__ = "t_knowledge_chunk"

    id = Column(Integer, primary_key=True, autoincrement=True)
    file_id = Column(Integer, nullable=False, comment="文件ID")
    chunk_index = Column(Integer, comment="分块索引")
    content = Column(Text, comment="分块内容")
    vector_id = Column(String(100), comment="向量ID")
    create_time = Column(DateTime, server_default=func.now())
```

更新 `models/__init__.py`，加上 `KnowledgeFile, KnowledgeChunk`。

## 6.2 文档加载器（rag/loader.py）

创建 `rag/__init__.py` 和 `rag/loader.py`：

```python
"""文档加载器 - 支持 txt/md/docx/pdf"""
import os
from PyPDF2 import PdfReader
from docx import Document


def load_document(file_path: str) -> str:
    """加载文档并提取纯文本"""
    ext = os.path.splitext(file_path)[1].lower()

    if ext == ".txt":
        return _load_txt(file_path)
    elif ext == ".md":
        return _load_markdown(file_path)
    elif ext == ".docx":
        return _load_docx(file_path)
    elif ext == ".pdf":
        return _load_pdf(file_path)
    else:
        raise ValueError(f"不支持的文件格式: {ext}")


def _load_txt(file_path: str) -> str:
    with open(file_path, "r", encoding="utf-8") as f:
        return f.read()


def _load_markdown(file_path: str) -> str:
    with open(file_path, "r", encoding="utf-8") as f:
        return f.read()


def _load_docx(file_path: str) -> str:
    doc = Document(file_path)
    paragraphs = [p.text for p in doc.paragraphs]
    return "\n".join(paragraphs)


def _load_pdf(file_path: str) -> str:
    reader = PdfReader(file_path)
    text_parts = []
    for page in reader.pages:
        text_parts.append(page.extract_text() or "")
    return "\n".join(text_parts)
```

## 6.3 文本分块器（rag/splitter.py）

```python
"""文本分块器"""
from app.core.config import settings


def split_text(text: str, chunk_size: int = None, chunk_overlap: int = None) -> list[str]:
    """将文本按固定大小分块，块之间有重叠"""
    chunk_size = chunk_size or settings.chunk_size
    chunk_overlap = chunk_overlap or settings.chunk_overlap

    if not text or len(text.strip()) == 0:
        return []

    text = text.strip()
    chunks = []
    start = 0
    text_len = len(text)

    while start < text_len:
        end = min(start + chunk_size, text_len)
        chunk = text[start:end].strip()
        if chunk:
            chunks.append(chunk)
        start += chunk_size - chunk_overlap
        if start >= text_len:
            break

    return chunks
```

## 6.4 向量化模块（rag/embeddings.py）

```python
"""向量化模块 - 调用通义千问Embedding API"""
from langchain_openai import OpenAIEmbeddings
from app.core.config import settings


def get_embeddings():
    """获取Embedding模型实例"""
    return OpenAIEmbeddings(
        model=settings.embedding_model,
        openai_api_key=settings.openai_api_key,
        openai_api_base=settings.openai_base_url,
        dimensions=settings.embedding_dimensions,
        chunk_size=settings.embedding_batch_size,
    )
```

## 6.5 向量存储（rag/vector_store.py）

```python
"""向量存储 - Chroma数据库"""
from langchain_community.vectorstores import Chroma
from app.core.config import settings
from app.rag.embeddings import get_embeddings

_vector_store = None


def get_vector_store():
    """获取向量存储实例（单例）"""
    global _vector_store
    if _vector_store is None:
        embeddings = get_embeddings()
        _vector_store = Chroma(
            collection_name=settings.chroma_collection,
            embedding_function=embeddings,
            persist_directory=settings.chroma_persist_dir,
        )
    return _vector_store


def add_texts(texts: list[str], metadatas: list[dict], ids: list[str]):
    """批量添加文本到向量库"""
    vs = get_vector_store()
    vs.add_texts(texts=texts, metadatas=metadatas, ids=ids)
    vs.persist()


def delete_by_file_id(file_id: int):
    """根据文件ID删除向量"""
    vs = get_vector_store()
    collection = vs._collection
    results = collection.get(where={"file_id": file_id})
    if results["ids"]:
        collection.delete(ids=results["ids"])
    vs.persist()


def similarity_search(query: str, k: int = None) -> list:
    """相似度检索，返回 (doc, score) 列表"""
    k = k or settings.retrieval_top_k
    vs = get_vector_store()
    return vs.similarity_search_with_score(query, k=k)
```

## 6.6 RAG 服务（services/rag_service.py）

```python
"""RAG检索增强服务"""
import os
import sys
from sqlalchemy.orm import Session

from app.core.config import settings
from app.rag.loader import load_document
from app.rag.splitter import split_text
from app.rag.vector_store import get_vector_store, add_texts, delete_by_file_id
from app.models.knowledge import KnowledgeFile, KnowledgeChunk
from app.services.graph_service import get_graph_service


def _safe_print(message: str) -> None:
    """安全打印（兼容Windows GBK控制台）"""
    try:
        encoding = getattr(sys.stdout, "encoding", None) or "utf-8"
        print(message.encode(encoding, errors="replace").decode(encoding, errors="replace"), flush=True)
    except Exception:
        pass


class RagService:
    """RAG检索增强生成服务"""

    def __init__(self):
        self.vector_store = get_vector_store()
        self.graph_service = get_graph_service()

    def process_file(self, db: Session, file_record: KnowledgeFile) -> int:
        """处理知识库文件：解析、分块、向量化，返回分块数量"""
        file_record.vector_status = 1  # 处理中
        db.commit()
        try:
            # 1. 解析文件
            file_path = file_record.file_path.replace("/uploads33/", "")
            abs_path = os.path.join(settings.upload_dir, file_path)
            text = load_document(abs_path)
            _safe_print(f"[RAG] 文件解析完成: {file_record.file_name}, 长度={len(text)}")

            # 2. 文本分块
            chunks = split_text(text)
            _safe_print(f"[RAG] 文本分块完成: {len(chunks)} 块")

            # 3. 清除旧数据
            db.query(KnowledgeChunk).filter(KnowledgeChunk.file_id == file_record.id).delete()
            delete_by_file_id(file_record.id)

            # 4. 写入MySQL分块表 + 准备向量数据
            texts, metadatas, ids = [], [], []
            for idx, chunk in enumerate(chunks):
                vector_id = f"file_{file_record.id}_chunk_{idx}"
                db_chunk = KnowledgeChunk(
                    file_id=file_record.id,
                    chunk_index=idx,
                    content=chunk,
                    vector_id=vector_id,
                )
                db.add(db_chunk)
                texts.append(chunk)
                metadatas.append({
                    "file_id": file_record.id,
                    "file_name": file_record.file_name,
                    "chunk_index": idx,
                })
                ids.append(vector_id)

            # 5. 写入Chroma向量库
            if texts:
                add_texts(texts, metadatas, ids)
                _safe_print(f"[RAG] 向量化完成: {len(texts)} 个向量")

            # 6. 更新状态
            file_record.vector_status = 2
            file_record.chunk_count = len(chunks)
            db.commit()
            return len(chunks)

        except Exception as e:
            file_record.vector_status = 3
            file_record.error_msg = str(e)[:500]
            db.commit()
            _safe_print(f"[RAG] 向量化失败: {str(e)}")
            raise

    def retrieve(self, query: str, k: int = None) -> list[dict]:
        """检索相关知识片段"""
        k = k or settings.retrieval_top_k
        results = self.vector_store.similarity_search_with_score(query, k=k)
        references = []
        for doc, score in results:
            references.append({
                "content": doc.page_content,
                "file_name": doc.metadata.get("file_name", ""),
                "chunk_index": doc.metadata.get("chunk_index", 0),
                "score": float(score),
            })
        return references


_rag_service = None


def get_rag_service():
    """单例获取RAG服务"""
    global _rag_service
    if _rag_service is None:
        _rag_service = RagService()
    return _rag_service
```

## 6.7 知识库接口（api/v1/knowledge.py）

```python
"""知识库管理接口"""
import os
import uuid
from fastapi import APIRouter, Depends, UploadFile, File, Form
from sqlalchemy.orm import Session

from app.core.deps import require_roles, CurrentUser
from app.core.response import success, page_result
from app.core.config import settings
from app.db.session import get_db
from app.models.knowledge import KnowledgeFile
from app.services.rag_service import get_rag_service

router = APIRouter()

ALLOWED_EXTENSIONS = {".txt", ".md", ".docx", ".pdf"}


def _allowed_file(filename: str) -> bool:
    ext = os.path.splitext(filename)[1].lower()
    return ext in ALLOWED_EXTENSIONS


@router.get("")
def list_files(page: int = 1, page_size: int = 10, keyword: str = "", db: Session = Depends(get_db),
               current: CurrentUser = Depends(require_roles("admin"))):
    """知识库文件列表"""
    query = db.query(KnowledgeFile)
    if keyword:
        query = query.filter(KnowledgeFile.file_name.like(f"%{keyword}%"))
    total = query.count()
    items = query.order_by(KnowledgeFile.id.desc()).offset((page - 1) * page_size).limit(page_size).all()
    data = [{
        "id": f.id, "file_name": f.file_name, "file_size": f.file_size,
        "file_type": f.file_type, "chunk_count": f.chunk_count,
        "vector_status": f.vector_status, "error_msg": f.error_msg,
        "create_time": f.create_time.strftime("%Y-%m-%d %H:%M:%S"),
    } for f in items]
    return page_result(data, total, page, page_size)


@router.post("/upload")
async def upload_file(file: UploadFile = File(...), db: Session = Depends(get_db),
                      current: CurrentUser = Depends(require_roles("admin"))):
    """上传知识库文件并触发向量化"""
    if not _allowed_file(file.filename):
        from fastapi import HTTPException
        raise HTTPException(status_code=400, detail="仅支持 txt/md/docx/pdf 格式")

    # 保存文件
    ext = os.path.splitext(file.filename)[1].lower()
    save_name = f"{uuid.uuid4().hex}{ext}"
    save_path = os.path.join(settings.upload_dir, "knowledge", save_name)
    os.makedirs(os.path.dirname(save_path), exist_ok=True)

    content = await file.read()
    with open(save_path, "wb") as f:
        f.write(content)

    # 保存记录
    file_record = KnowledgeFile(
        file_name=file.filename,
        file_path=f"/uploads33/knowledge/{save_name}",
        file_size=len(content),
        file_type=ext.lstrip("."),
    )
    db.add(file_record)
    db.commit()
    db.refresh(file_record)

    # 异步向量化（这里简化为同步，生产环境建议用任务队列）
    try:
        get_rag_service().process_file(db, file_record)
    except Exception:
        pass  # 状态已在service中更新

    return success({"id": file_record.id, "file_name": file_record.file_name})


@router.delete("/{file_id}")
def delete_file(file_id: int, db: Session = Depends(get_db),
                current: CurrentUser = Depends(require_roles("admin"))):
    """删除知识库文件"""
    file_record = db.query(KnowledgeFile).filter(KnowledgeFile.id == file_id).first()
    if not file_record:
        from fastapi import HTTPException
        raise HTTPException(status_code=404, detail="文件不存在")
    # 删除物理文件
    try:
        file_path = file_record.file_path.replace("/uploads33/", "")
        abs_path = os.path.join(settings.upload_dir, file_path)
        if os.path.exists(abs_path):
            os.remove(abs_path)
    except Exception:
        pass
    # 删除向量
    from app.rag.vector_store import delete_by_file_id
    delete_by_file_id(file_id)
    # 删除数据库记录
    db.delete(file_record)
    db.commit()
    return success()
```

## 6.8 注册路由

在 `api/v1/__init__.py` 中加上：

```python
from app.api.v1.knowledge import router as knowledge_router

api_router.include_router(knowledge_router, prefix="/knowledge", tags=["知识库"])
```

## 6.9 本地验证

1. 启动后端，用管理员账号登录获取 token
2. 在 Swagger 中点击「Authorize」，输入 `Bearer <token>`
3. 测试上传文件：`POST /api/v1/knowledge/upload`，选一个 txt 或 md 文件
4. 测试文件列表：`GET /api/v1/knowledge`，看 vector_status 是否为 2（成功）

## 6.10 Docker 验证

```bash
docker-compose up -d --build server
docker-compose logs -f server
```

确认启动成功后，同样上传一个文件测试向量化是否正常。

> **注意：** Docker 容器里的 uploads 目录存在 volume 里，删除容器不会丢失文件。

## 6.11 Git 提交

```bash
git add .
git commit -m "阶段六：RAG检索增强模块 - 文档加载/分块/向量化/检索 + 知识库管理"
git push
```

---

# 阶段七：AI 智能问诊模块

> **本阶段目标：** 实现 AI 问诊的 SSE 流式对话，融合 RAG 检索与图谱推理结果。

## 7.1 问诊数据模型（models/consult.py）

```python
"""AI问诊会话与消息模型"""
from sqlalchemy import Column, Integer, String, DateTime, Text, ForeignKey
from sqlalchemy.sql import func

from app.db.session import Base


class ConsultSession(Base):
    """问诊会话表"""
    __tablename__ = "t_consult_session"

    id = Column(Integer, primary_key=True, autoincrement=True)
    user_id = Column(Integer, nullable=False, comment="用户ID")
    title = Column(String(200), comment="会话标题")
    message_count = Column(Integer, default=0, comment="消息数")
    create_time = Column(DateTime, server_default=func.now())
    update_time = Column(DateTime, server_default=func.now(), onupdate=func.now())


class ConsultMessage(Base):
    """问诊消息表"""
    __tablename__ = "t_consult_message"

    id = Column(Integer, primary_key=True, autoincrement=True)
    session_id = Column(Integer, ForeignKey("t_consult_session.id"), nullable=False)
    role = Column(String(20), nullable=False, comment="user/assistant")
    content = Column(Text, comment="消息内容")
    references_json = Column(Text, comment="引用文献JSON")
    graph_json = Column(Text, comment="图谱推理结果JSON")
    create_time = Column(DateTime, server_default=func.now())
```

更新 `models/__init__.py`，加上 `ConsultSession, ConsultMessage`。

## 7.2 Pydantic 模型补充

在 `schemas/common.py` 中加上：

```python
class ChatRequest(BaseModel):
    """AI问诊请求"""
    message: str = Field(..., description="用户消息")
    session_id: Optional[int] = Field(None, description="会话ID，新会话不传")
```

## 7.3 完善 RAG 服务 - 流式对话

在 `services/rag_service.py` 的 `RagService` 类中添加流式对话方法：

```python
import json
import time
from typing import AsyncGenerator
from langchain_openai import ChatOpenAI

# 在 __init__ 中加上 self.llm
def __init__(self):
    self.llm = ChatOpenAI(
        model=settings.llm_model,
        openai_api_key=settings.openai_api_key,
        openai_api_base=settings.openai_base_url,
        streaming=True,
        temperature=0.7,
    )
    self.vector_store = get_vector_store()
    self.graph_service = get_graph_service()

# 添加 chat_stream 方法
async def chat_stream(self, message: str, history: list[dict]) -> AsyncGenerator[str, None]:
    """
    流式对话 - 融合RAG检索与图谱推理
    返回 SSE 格式的字符串流
    """
    start_time = time.time()

    # 1. 向量检索
    references = self.retrieve(message)
    ref_texts = [r["content"] for r in references]
    context_str = "\n\n".join(f"[参考资料{i+1}]\n{r}" for i, r in enumerate(ref_texts))

    # 2. 图谱推理（提取症状关键词，简化处理）
    graph_results = []
    try:
        # 简单关键词匹配（实际项目可用NER提取症状）
        all_symptoms = self.graph_service.get_all_symptoms()
        matched = [s for s in all_symptoms if s in message]
        if matched:
            graph_results = self.graph_service.infer_diseases_by_symptoms(matched[:5])
    except Exception:
        pass

    graph_str = ""
    if graph_results:
        graph_str = "\n\n【图谱推理结果】\n"
        for i, g in enumerate(graph_results[:5]):
            graph_str += f"{i+1}. {g['disease']}（匹配{g['match_count']}个症状）- 建议科室：{g['department']}\n"

    # 3. 构建系统提示词
    system_prompt = f"""你是一位专业的AI医疗助手，请基于以下参考资料回答用户的问题。

【重要声明】
- 你的回答仅供健康咨询参考，不能替代执业医师的诊断和治疗建议
- 如果症状严重或持续不缓解，请及时就医
- 不要给出具体的用药剂量指导，建议遵医嘱

【参考资料】
{context_str}
{graph_str}

请用中文回答，条理清晰，先给出核心结论，再分点说明。回答末尾要加上风险提示。"""

    # 4. 构建消息列表
    messages = [{"role": "system", "content": system_prompt}]
    # 加入历史（只保留最近10轮，避免上下文过长）
    for h in history[-10:]:
        messages.append({"role": h["role"], "content": h["content"]})
    messages.append({"role": "user", "content": message})

    # 5. 流式生成
    full_content = ""
    try:
        async for chunk in self.llm.astream(messages):
            if chunk.content:
                full_content += chunk.content
                # SSE 格式：data: {json}\n\n
                yield f"data: {json.dumps({'type': 'content', 'content': chunk.content}, ensure_ascii=False)}\n\n"
    except Exception as e:
        yield f"data: {json.dumps({'type': 'error', 'message': str(e)}, ensure_ascii=False)}\n\n"
        return

    cost_time = round(time.time() - start_time, 2)

    # 6. 推送完成事件
    yield f"data: {json.dumps({
        'type': 'done',
        'references': references,
        'graph_results': graph_results,
        'cost_time': cost_time,
    }, ensure_ascii=False)}\n\n"
```

> **SSE 事件类型说明：**
> - `session`：会话ID（新会话时推送）
> - `content`：AI 生成的内容片段（逐字推送）
> - `done`：生成完成，附带引用和图谱结果
> - `error`：出错了

## 7.4 问诊接口（api/v1/chat.py）

```python
"""AI问诊接口 - SSE流式"""
import json
from fastapi import APIRouter, Depends
from fastapi.responses import StreamingResponse
from sqlalchemy.orm import Session

from app.core.deps import require_roles, CurrentUser
from app.core.response import success
from app.db.session import get_db
from app.models.consult import ConsultSession, ConsultMessage
from app.schemas.common import ChatRequest
from app.services.rag_service import get_rag_service
from app.utils.helpers import format_datetime

router = APIRouter()


@router.get("/sessions")
def list_sessions(db: Session = Depends(get_db),
                  current: CurrentUser = Depends(require_roles("user"))):
    """获取用户会话列表"""
    items = db.query(ConsultSession).filter(
        ConsultSession.user_id == current.user_id
    ).order_by(ConsultSession.update_time.desc()).all()
    data = [{
        "id": s.id, "title": s.title, "message_count": s.message_count,
        "create_time": format_datetime(s.create_time),
    } for s in items]
    return success(data)


@router.get("/sessions/{session_id}/messages")
def get_messages(session_id: int, db: Session = Depends(get_db),
                 current: CurrentUser = Depends(require_roles("user"))):
    """获取会话消息列表"""
    session = db.query(ConsultSession).filter(
        ConsultSession.id == session_id, ConsultSession.user_id == current.user_id
    ).first()
    if not session:
        from fastapi import HTTPException
        raise HTTPException(status_code=404, detail="会话不存在")
    msgs = db.query(ConsultMessage).filter(
        ConsultMessage.session_id == session_id
    ).order_by(ConsultMessage.id).all()
    data = [{
        "id": m.id, "role": m.role, "content": m.content,
        "references_json": m.references_json, "graph_json": m.graph_json,
        "create_time": format_datetime(m.create_time),
    } for m in msgs]
    return success(data)


@router.post("/send")
async def chat_send(req: ChatRequest, db: Session = Depends(get_db),
                    current: CurrentUser = Depends(require_roles("user"))):
    """AI问诊 - SSE流式回复"""
    # 1. 获取或创建会话
    if req.session_id:
        session = db.query(ConsultSession).filter(
            ConsultSession.id == req.session_id,
            ConsultSession.user_id == current.user_id
        ).first()
    else:
        session = None
    if not session:
        title = req.message[:20] + ("..." if len(req.message) > 20 else "")
        session = ConsultSession(user_id=current.user_id, title=title)
        db.add(session)
        db.commit()
        db.refresh(session)

    # 2. 保存用户消息
    user_msg = ConsultMessage(session_id=session.id, role="user", content=req.message)
    db.add(user_msg)
    session.message_count += 1
    db.commit()

    # 3. 获取历史消息
    history_msgs = db.query(ConsultMessage).filter(
        ConsultMessage.session_id == session.id
    ).order_by(ConsultMessage.id).all()
    history = [{"role": m.role, "content": m.content} for m in history_msgs[:-1]]

    session_id = session.id

    async def event_generator():
        # 先推送会话ID
        yield f"data: {json.dumps({'type': 'session', 'session_id': session_id}, ensure_ascii=False)}\n\n"

        full_content = ""
        references = []
        graph_results = []

        try:
            async for chunk in get_rag_service().chat_stream(req.message, history):
                yield chunk
                # 收集完整内容和引用
                if chunk.startswith("data: "):
                    try:
                        data = json.loads(chunk[6:].strip())
                        if data.get("type") == "content":
                            full_content += data["content"]
                        elif data.get("type") == "done":
                            references = data.get("references", [])
                            graph_results = data.get("graph_results", [])
                    except Exception:
                        pass

            # 保存AI回答到数据库
            ai_msg = ConsultMessage(
                session_id=session_id,
                role="assistant",
                content=full_content,
                references_json=json.dumps(references, ensure_ascii=False),
                graph_json=json.dumps(graph_results, ensure_ascii=False),
            )
            db.add(ai_msg)
            # 更新会话消息数（注意：在生成器里操作db需要小心）
            session_obj = db.query(ConsultSession).filter(ConsultSession.id == session_id).first()
            if session_obj:
                session_obj.message_count += 1
            db.commit()

        except Exception as e:
            yield f"data: {json.dumps({'type': 'error', 'message': str(e)}, ensure_ascii=False)}\n\n"

    return StreamingResponse(event_generator(), media_type="text/event-stream")
```

## 7.5 注册路由

在 `api/v1/__init__.py` 中加上：

```python
from app.api.v1.chat import router as chat_router

api_router.include_router(chat_router, prefix="/chat", tags=["AI问诊"])
```

## 7.6 本地验证

1. 用患者账号登录获取 token
2. 用 curl 或 Postman 测试 SSE 接口（Swagger 对 SSE 支持不好）：

```bash
curl -N -X POST http://localhost:8000/api/v1/chat/send \
  -H "Authorization: Bearer <你的token>" \
  -H "Content-Type: application/json" \
  -d '{"message": "头痛头晕是怎么回事？"}'
```

应该能看到逐字返回的流式数据。

## 7.7 Docker 验证

```bash
docker-compose up -d --build server
docker-compose logs -f server
```

确认启动后，用上面的 curl 命令测试。

## 7.8 Git 提交

```bash
git add .
git commit -m "阶段七：AI智能问诊模块 - SSE流式对话 + RAG+图谱融合"
git push
```

---

# 阶段八：业务功能模块

> **本阶段目标：** 完成预约挂号、在线咨询、健康档案、科普文章、数据统计等业务功能。
> 由于模块较多，这里给出核心结构，具体实现参考项目源码。

## 8.1 涉及的模型文件

需要创建以下模型（参考项目源码对应文件）：

| 模型文件 | 对应表 | 说明 |
|---------|-------|------|
| `models/department.py` | t_department | 科室 |
| `models/appointment.py` | t_appointment | 预约挂号 |
| `models/doctor_consult.py` | t_doctor_consult / t_doctor_consult_message | 医生在线咨询 |
| `models/article.py` | t_article | 健康科普文章 |
| `models/notice.py` | t_notice | 公告 |
| `models/health_record.py` | t_health_record | 健康档案 |

## 8.2 涉及的接口文件

| 接口文件 | 路由前缀 | 主要功能 |
|---------|---------|---------|
| `api/v1/department.py` | /departments | 科室CRUD |
| `api/v1/doctor.py` | /doctors | 医生管理 |
| `api/v1/user.py` | /users | 用户管理 |
| `api/v1/appointment.py` | /appointments | 预约挂号 |
| `api/v1/consult.py` | /consults | 医生在线咨询 |
| `api/v1/article.py` | /articles | 健康科普 |
| `api/v1/notice.py` | /notices | 公告 |
| `api/v1/record.py` | /records | 健康档案 |
| `api/v1/profile.py` | /profile | 个人中心（三角色通用） |
| `api/v1/stat.py` | /stat | 数据统计 |

## 8.3 服务层文件

| 服务文件 | 说明 |
|---------|------|
| `services/stat_service.py` | 统计数据聚合 |

## 8.4 开发建议

1. **按模块逐个开发**：先做科室管理，再做医生管理，然后预约、咨询...
2. **每个模块写完就测试**：用 Swagger 测通再写下一个
3. **写完就 Git 提交**：保持提交粒度小，方便回退

## 8.5 Docker 验证

每完成几个模块，重新构建后端镜像验证：

```bash
docker-compose up -d --build server
```

## 8.6 Git 提交

```bash
git add .
git commit -m "阶段八：业务功能模块 - 预约/咨询/档案/文章/统计等"
git push
```

---

# 阶段九：前端工程模块

> **本阶段目标：** 搭建 Vue3 前端工程，实现三角色页面。
> **Docker 状态：** 本阶段末创建前端 Dockerfile，加入 docker-compose。

## 9.1 前端项目初始化

```bash
cd .
npm create vite@latest client -- --template vue
cd client
npm install
```

安装依赖：
```bash
npm install element-plus @element-plus/icons-vue vue-router pinia axios echarts vue-echarts markdown-it
```

## 9.2 Vite 配置（vite.config.js）

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { fileURLToPath, URL } from 'node:url'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:8000',
        changeOrigin: true,
      },
      '/uploads33': {
        target: 'http://127.0.0.1:8000',
        changeOrigin: true,
      },
    },
  },
})
```

## 9.3 入口文件（src/main.js）

```javascript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import * as ElementPlusIconsVue from '@element-plus/icons-vue'
import App from './App.vue'
import router from './router'
import './styles/global.css'

const app = createApp(App)

for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)
}

app.use(createPinia())
app.use(router)
app.use(ElementPlus)
app.mount('#app')
```

## 9.4 HTTP 请求封装（src/utils/request.js）

```javascript
import axios from 'axios'
import { ElMessage } from 'element-plus'
import { useUserStore } from '@/stores/user'

const request = axios.create({
  baseURL: '/api/v1',
  timeout: 30000,
})

// 请求拦截器
request.interceptors.request.use(config => {
  const userStore = useUserStore()
  if (userStore.token) {
    config.headers.Authorization = `Bearer ${userStore.token}`
  }
  return config
})

// 响应拦截器
request.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code !== 200) {
      ElMessage.error(res.message || '请求失败')
      return Promise.reject(new Error(res.message))
    }
    return res.data
  },
  error => {
    if (error.response?.status === 401) {
      const userStore = useUserStore()
      userStore.logout()
      window.location.href = '/login'
    }
    ElMessage.error(error.message || '网络错误')
    return Promise.reject(error)
  }
)

export default request
```

## 9.5 状态管理（src/stores/user.js）

```javascript
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useUserStore = defineStore('user', () => {
  const token = ref(localStorage.getItem('token') || '')
  const role = ref(localStorage.getItem('role') || '')
  const userInfo = ref(JSON.parse(localStorage.getItem('userInfo') || 'null'))

  function setLogin(data) {
    token.value = data.access_token
    role.value = data.role
    userInfo.value = data
    localStorage.setItem('token', data.access_token)
    localStorage.setItem('role', data.role)
    localStorage.setItem('userInfo', JSON.stringify(data))
  }

  function logout() {
    token.value = ''
    role.value = ''
    userInfo.value = null
    localStorage.removeItem('token')
    localStorage.removeItem('role')
    localStorage.removeItem('userInfo')
  }

  return { token, role, userInfo, setLogin, logout }
})
```

## 9.6 路由配置（src/router/index.js）

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import { useUserStore } from '@/stores/user'

const routes = [
  { path: '/login', name: 'login', component: () => import('@/views/auth/Login.vue') },
  { path: '/register', name: 'register', component: () => import('@/views/auth/Register.vue') },
  // 患者端
  {
    path: '/',
    component: () => import('@/layouts/PortalLayout.vue'),
    children: [
      { path: '', name: 'home', component: () => import('@/views/portal/Home.vue') },
      { path: 'chat', name: 'chat', component: () => import('@/views/portal/Chat.vue'), meta: { requiresAuth: true, role: 'user' } },
      { path: 'symptom', name: 'symptom', component: () => import('@/views/portal/Symptom.vue') },
      { path: 'appointment', name: 'appointment', component: () => import('@/views/portal/Appointment.vue'), meta: { requiresAuth: true, role: 'user' } },
      { path: 'consult', name: 'consult', component: () => import('@/views/portal/Consult.vue'), meta: { requiresAuth: true, role: 'user' } },
      { path: 'records', name: 'records', component: () => import('@/views/portal/Records.vue'), meta: { requiresAuth: true, role: 'user' } },
      { path: 'articles', name: 'articles', component: () => import('@/views/portal/Articles.vue') },
      { path: 'articles/:id', name: 'article-detail', component: () => import('@/views/portal/ArticleDetail.vue') },
      { path: 'profile', name: 'portal-profile', component: () => import('@/views/portal/Profile.vue'), meta: { requiresAuth: true, role: 'user' } },
    ]
  },
  // 医生端
  {
    path: '/doctor',
    component: () => import('@/layouts/AdminLayout.vue'),
    meta: { requiresAuth: true, role: 'doctor' },
    children: [
      { path: '', name: 'doctor-dashboard', component: () => import('@/views/doctor/Dashboard.vue') },
      { path: 'consults', name: 'doctor-consults', component: () => import('@/views/doctor/Consults.vue') },
      { path: 'appointments', name: 'doctor-appointments', component: () => import('@/views/doctor/Appointments.vue') },
      { path: 'patients', name: 'doctor-patients', component: () => import('@/views/doctor/Patients.vue') },
      { path: 'profile', name: 'doctor-profile', component: () => import('@/views/doctor/Profile.vue') },
    ]
  },
  // 管理端
  {
    path: '/admin',
    component: () => import('@/layouts/AdminLayout.vue'),
    meta: { requiresAuth: true, role: 'admin' },
    children: [
      { path: '', name: 'admin-dashboard', component: () => import('@/views/admin/Dashboard.vue') },
      { path: 'users', name: 'admin-users', component: () => import('@/views/admin/Users.vue') },
      { path: 'doctors', name: 'admin-doctors', component: () => import('@/views/admin/Doctors.vue') },
      { path: 'departments', name: 'admin-departments', component: () => import('@/views/admin/Departments.vue') },
      { path: 'knowledge', name: 'admin-knowledge', component: () => import('@/views/admin/Knowledge.vue') },
      { path: 'graph', name: 'admin-graph', component: () => import('@/views/admin/Graph.vue') },
      { path: 'appointments', name: 'admin-appointments', component: () => import('@/views/admin/Appointments.vue') },
      { path: 'consults', name: 'admin-consults', component: () => import('@/views/admin/Consults.vue') },
      { path: 'articles', name: 'admin-articles', component: () => import('@/views/admin/Articles.vue') },
      { path: 'notices', name: 'admin-notices', component: () => import('@/views/admin/Notices.vue') },
      { path: 'profile', name: 'admin-profile', component: () => import('@/views/admin/Profile.vue') },
    ]
  },
]

const router = createRouter({
  history: createWebHistory(),
  routes,
})

// 路由守卫
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  const requiresAuth = to.meta.requiresAuth
  const requiredRole = to.meta.role

  if (requiresAuth) {
    if (!userStore.token) {
      next({ path: '/login', query: { redirect: to.fullPath } })
    } else if (requiredRole && userStore.role !== requiredRole) {
      next('/')
    } else {
      next()
    }
  } else {
    next()
  }
})

export default router
```

## 9.7 页面开发

按照路由配置，在 `src/views/` 下创建对应的页面组件。核心页面包括：

- **登录注册**：Login.vue、Register.vue
- **患者端**：Home.vue、Chat.vue（SSE流式对话）、Symptom.vue、Appointment.vue、Consult.vue、Records.vue、Articles.vue、ArticleDetail.vue、Profile.vue
- **医生端**：Dashboard.vue、Consults.vue、Appointments.vue、Patients.vue、Profile.vue
- **管理端**：Dashboard.vue（ECharts图表）、Users.vue、Doctors.vue、Departments.vue、Knowledge.vue、Graph.vue（图谱可视化）、Appointments.vue、Consults.vue、Articles.vue、Notices.vue、Profile.vue

> **提示：** 页面代码量较大，建议参考项目源码中的实现，重点理解：
> - Chat.vue 中 SSE 流式数据的消费方式（EventSource）
> - Graph.vue 中 ECharts 关系图的配置
> - Dashboard.vue 中统计图表的实现

## 9.8 前端 Dockerfile

在 `frontend/` 下创建 `Dockerfile`：

```dockerfile
# ==========================================
# 前端 Dockerfile - 多阶段构建
# ==========================================

# 阶段一：构建
FROM node:18-alpine as builder

WORKDIR /app

COPY package*.json ./
RUN npm install --registry=https://registry.npmmirror.com

COPY . .
RUN npm run build

# 阶段二：生产环境（nginx 托管静态文件）
FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

## 9.9 Nginx 配置

在 `frontend/` 下创建 `nginx.conf`：

```nginx
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # 前端 history 模式路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 代理 API 请求到后端服务
    location /api/ {
        proxy_pass http://server:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # SSE 相关配置
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_buffering off;
        proxy_cache off;
    }

    # 代理上传文件
    location /uploads33/ {
        proxy_pass http://server:8000/uploads33/;
        proxy_set_header Host $host;
    }
}
```

> **SSE 注意事项：** Nginx 代理 SSE 时必须关闭缓冲（`proxy_buffering off`），否则会等响应全部完成才推送，就不是流式了。

## 9.10 更新 docker-compose.yml，加入前端

在 `docker-compose.yml` 的 `services` 中添加前端服务：

```yaml
  # ==========================================
  # 前端服务
  # ==========================================
  client:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: medical-client
    restart: unless-stopped
    depends_on:
      - server
    ports:
      - "80:80"
```

## 9.11 本地开发验证

```bash
cd frontend
npm run dev
```

访问 `http://localhost:5173`，测试登录、注册、AI问诊等功能。

## 9.12 Docker 全量验证

```bash
# 构建并启动所有服务
docker-compose up -d --build

# 查看状态
docker-compose ps

# 应该看到 4 个服务都在运行：
# medical-mysql    Up
# medical-neo4j    Up
# medical-server   Up
# medical-client   Up
```

访问 `http://localhost` 测试前端页面。

## 9.13 Git 提交

```bash
git add .
git commit -m "阶段九：前端工程模块 - Vue3三角色页面 + 前端Docker化"
git push
```

---

# 阶段十：全量容器化部署与运维

> **本阶段目标：** 完善 docker-compose，补充运维相关的配置和命令。

## 10.1 最终版 docker-compose.yml

完整的 docker-compose.yml 应该包含以下服务：

```yaml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: medical-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      TZ: Asia/Shanghai
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database:/docker-entrypoint-initdb.d
    command: >
      --default-authentication-plugin=mysql_native_password
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5

  neo4j:
    image: neo4j:5.16-community
    container_name: medical-neo4j
    restart: unless-stopped
    environment:
      NEO4J_AUTH: ${NEO4J_USER}/${NEO4J_PASSWORD}
      NEO4J_dbms_memory_heap_initial__size: 512M
      NEO4J_dbms_memory_heap_max__size: 1G
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - neo4j_data:/data
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:7474 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5

  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: medical-server
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      neo4j:
        condition: service_healthy
    environment:
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=${NEO4J_USER}
      - NEO4J_PASSWORD=${NEO4J_PASSWORD}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL}
      - LLM_MODEL=${LLM_MODEL}
      - EMBEDDING_MODEL=${EMBEDDING_MODEL}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - JWT_ALGORITHM=${JWT_ALGORITHM}
      - JWT_EXPIRE_MINUTES=${JWT_EXPIRE_MINUTES}
      - UPLOAD_DIR=/app/uploads
      - CHROMA_PERSIST_DIR=/app/chroma_db
    ports:
      - "8000:8000"
    volumes:
      - uploads_data:/app/uploads
      - chroma_data:/app/chroma_db
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  client:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: medical-client
    restart: unless-stopped
    depends_on:
      - server
    ports:
      - "80:80"

volumes:
  mysql_data:
  neo4j_data:
  uploads_data:
  chroma_data:
```

> **新增了什么？**
> - `healthcheck`：健康检查，确保依赖的服务就绪后再启动
> - `depends_on.condition`：等待健康检查通过再启动

## 10.2 部署步骤

### 服务器部署流程

1. **上传代码到服务器**
   ```bash
   # 在服务器上克隆仓库
   git clone 你的仓库地址
   cd 项目目录
   ```

2. **创建 .env 文件**
   ```bash
   cp .env.example .env
   nano .env  # 填入真实配置
   ```

3. **启动所有服务**
   ```bash
   docker-compose up -d --build
   ```

4. **初始化知识图谱**
   ```bash
   docker exec -it medical-server bash
   python scripts/init_graph.py
   exit
   ```

5. **验证访问**
   - 前端：http://服务器IP
   - 后端API文档：http://服务器IP:8000/docs
   - Neo4j Browser：http://服务器IP:7474

## 10.3 常用运维命令

```bash
# 查看所有服务状态
docker-compose ps

# 查看日志
docker-compose logs -f           # 所有服务
docker-compose logs -f server    # 只看后端
docker-compose logs -f client    # 只看前端
docker-compose logs -f mysql     # 只看MySQL

# 启动/停止/重启
docker-compose start             # 启动所有
docker-compose stop              # 停止所有
docker-compose restart server    # 重启后端

# 重新构建并启动某个服务
docker-compose up -d --build server

# 进入容器
docker exec -it medical-server bash
docker exec -it medical-mysql mysql -u root -p

# 备份数据库
docker exec medical-mysql mysqldump -u root -p123456 db_ai_medical > backup_$(date +%Y%m%d).sql

# 完全删除所有容器和数据卷（慎用！）
docker-compose down -v
```

## 10.4 Git 最终提交

```bash
git add .
git commit -m "阶段十：全量容器化部署 - docker-compose完善 + 运维文档"
git push
```

---

# 附录 A：Git 常用命令速查

```bash
# 查看状态
git status

# 查看修改内容
git diff
git diff 文件名

# 提交
git add .
git commit -m "提交说明"

# 推送/拉取
git push
git pull

# 查看历史
git log
git log --oneline

# 回退
git reset --soft HEAD~1   # 撤销最近一次提交，改动保留
git reset --hard HEAD~1   # 撤销最近一次提交，改动也删了（慎用）

# 分支
git branch               # 查看分支
git branch dev           # 创建dev分支
git checkout dev         # 切换到dev分支
git merge dev            # 把dev合并到当前分支

# 临时保存
git stash                # 暂存改动
git stash pop            # 恢复暂存
```

---

# 附录 B：常见问题

## Q1：启动后端报错「ModuleNotFoundError」

A：依赖没装全，执行 `pip install -r requirements.txt`。

## Q2：连接 MySQL 失败

A：检查 MySQL 容器是否启动（`docker-compose ps`），端口、用户名、密码是否正确。

## Q3：AI 问诊没反应

A：检查 `OPENAI_API_KEY` 是否设置，网络能否访问 API。查看后端日志找具体错误：`docker-compose logs -f server`。

## Q4：Neo4j 连接不上

A：确认 Neo4j 容器在运行，7687 端口是否通。可以先访问 `http://localhost:7474` 测试 Neo4j Browser。

## Q5：前端页面空白

A：F12 打开控制台看报错。常见原因：后端没启动、接口地址不对、跨域问题。

## Q6：Docker 启动后访问不了

A：先用 `docker-compose ps` 看容器状态，再用 `docker-compose logs` 看日志。

## Q7：SSE 流式输出不流畅

A：检查 Nginx 配置是否关闭了缓冲（`proxy_buffering off`）。本地开发时直接连后端不会有这个问题。

## Q8：Git 提交时不小心把 .env 提交了怎么办

A：
```bash
# 从 Git 中删除但保留本地文件
git rm --cached .env
# 提交
git commit -m "移除敏感文件"
# 确认 .gitignore 里有 .env
```

---

> **文档说明：** 本文档是项目操作的教学指南，按照「基础设施先行、逐步迭代、每步提交」的原则组织。
> 每个阶段完成后都有验证方法和 Git 提交，建议按顺序逐步实现，不要跳步。
> 完整的代码实现请参考项目源码中的对应文件。



